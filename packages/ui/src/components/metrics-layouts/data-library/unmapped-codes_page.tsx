import { ProductAreaPage } from "@/app/(app)/product-area-page"

export default function UnmappedCodesPage() {
  return (
    <ProductAreaPage
      title="Unmapped Codes"
      description="Review queue for source codes that need standard mapping."
    />
  )
}
