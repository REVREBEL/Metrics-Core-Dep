import { ProductAreaPage } from "@/app/(app)/product-area-page"

export default function CampaignSetupPage() {
  return (
    <ProductAreaPage
      title="Campaign Setup"
      description="Rate codes, promo codes, booking links, tags, and campaign goals."
    />
  )
}
