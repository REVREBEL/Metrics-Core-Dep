import { ProductAreaPage } from "@/app/(app)/product-area-page"

export default function ActiveCampaignsPage() {
  return (
    <ProductAreaPage
      title="Active Campaigns"
      description="Campaigns currently running across the selected property context."
    />
  )
}
