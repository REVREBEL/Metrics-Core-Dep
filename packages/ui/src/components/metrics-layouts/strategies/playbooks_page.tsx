import { ProductAreaPage } from "@/app/(app)/product-area-page"

export default function PlaybooksPage() {
  return (
    <ProductAreaPage
      title="Playbooks"
      description="Structured strategy playbooks for repeatable revenue actions."
    />
  )
}
